const state = { token: '', account: null, accounts: [], tasks: [], inbox: [] };
const app = document.querySelector('#app');
const api = async (path, options = {}) => { const headers = { 'Content-Type': 'application/json', ...(options.headers || {}) }; if (state.token) headers.Authorization = `Bearer ${state.token}`; const response = await fetch(path, { ...options, headers }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.detail || 'Request failed'); return body; };
const escapeHtml = (value) => String(value).replace(/[&<>'"]/g, (character) => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', "'":'&#39;', '"':'&quot;' }[character]));
const formData = (form) => Object.fromEntries(new FormData(form).entries());
const setError = (message) => { const node = document.querySelector('.error'); if (node) node.textContent = message; else app.insertAdjacentHTML('beforeend', `<div class="notice error">${escapeHtml(message)}</div>`); };

async function boot() { try { const status = await api('/auth/status'); status.hasAccounts ? renderLogin() : renderSetup(); } catch (error) { setError(error.message); } }
function renderSetup() { app.innerHTML = `<div class="auth"><div class="brand center"><div class="logo">✦</div><strong>Review Desk</strong></div><section class="card auth-card"><span class="eyebrow">First setup</span><h1 class="display">Create the administrator account.</h1><p class="muted">Only this account can create developers, tasks, and workspace data.</p><form id="setup" class="stack"><div class="field"><label>Name</label><input class="input" name="name" placeholder="Company administrator" required></div><div class="field"><label>Email</label><input class="input" name="email" type="email" required></div><div class="field"><label>Password</label><input class="input" name="password" type="password" minlength="4" required></div><button class="button">Create administrator</button></form></section></div>`; document.querySelector('#setup').onsubmit = async (event) => { event.preventDefault(); try { await api('/auth/setup', { method:'POST', body:JSON.stringify(formData(event.target)) }); renderLogin(); } catch (error) { setError(error.message); } }; }
function renderLogin() { app.innerHTML = `<div class="auth"><div class="brand center"><div class="logo">✦</div><strong>Review Desk</strong></div><section class="card auth-card"><span class="eyebrow">Company login</span><h1 class="display">Sign in to Review Desk.</h1><form id="login" class="stack"><div class="field"><label>Name or email</label><input class="input" name="identifier" placeholder="marwan or marwan@company.com" required></div><div class="field"><label>Password</label><input class="input" name="password" type="password" required></div><button class="button">Sign in</button></form><div class="notice" id="setup-note">First launch? Use the administrator setup endpoint in the API docs.</div></section></div>`; document.querySelector('#login').onsubmit = async (event) => { event.preventDefault(); try { const result = await api('/auth/login', { method:'POST', body: JSON.stringify(formData(event.target)) }); state.token = result.token; state.account = result.account; await renderDashboard(); } catch (error) { setError(error.message); } }; }
async function renderDashboard() { state.tasks = await api('/tasks'); state.accounts = state.account.accountType === 'administrator' ? await api('/accounts') : []; state.inbox = state.account.accountType === 'developer' ? await api('/review-requests/inbox') : []; const admin = state.account.accountType === 'administrator'; app.innerHTML = `<div class="layout"><aside class="sidebar side"><div class="brand"><div class="logo">✦</div><div><strong>Review Desk</strong><small>${escapeHtml(state.account.name)}</small></div></div><nav class="nav"><button class="active" data-view="home">${admin ? 'Administration' : 'My reviews'}</button>${admin ? '' : '<button data-view="start">Start a review</button>'}</nav><div class="help">Signed in as ${admin ? 'Administrator' : 'Developer'}. ${admin ? 'Only you can manage accounts and tasks.' : 'Review requests assigned to you appear here.'}</div><button class="button secondary" id="logout">Log out</button></aside><section class="content"><header class="header"><span class="eyebrow">${admin ? 'Administrator workspace' : 'Developer workspace'}</span><h1>${admin ? 'Manage the company workspace.' : 'Start or receive a review.'}</h1><p>${admin ? 'Create accounts and task instructions. Developers cannot access these controls.' : 'Choose a task, submit an element, or open a request assigned to you.'}</p></header><div id="view"></div></section></div>`; document.querySelector('#logout').onclick = () => { state.token=''; state.account=null; renderLogin(); }; document.querySelectorAll('[data-view]').forEach((button) => button.onclick = () => renderView(button.dataset.view)); renderView('home'); }
async function renderView(view) { const admin = state.account.accountType === 'administrator'; document.querySelector('#view').innerHTML = admin ? adminView() : (view === 'start' ? startView() : inboxView()); bindView(); }
function adminView() { return `<div class="grid"><section class="card"><h2>Create account</h2><p class="muted">Only administrators can create accounts.</p><form id="account-form" class="stack"><div class="field"><label>Name</label><input class="input" name="name" required></div><div class="field"><label>Email</label><input class="input" name="email" type="email" required></div><div class="field"><label>Password</label><input class="input" name="password" type="password" minlength="4" required></div><div class="field"><label>Role</label><select class="select" name="accountType"><option value="developer">Developer</option><option value="administrator">Administrator</option></select></div><button class="button">Create account</button></form></section><section class="card"><h2>Create task</h2><p class="muted">The preparation guide is shared with developers.</p><form id="task-form" class="stack"><div class="field"><label>Task name</label><input class="input" name="name" required></div><div class="field"><label>How to prepare</label><textarea class="textarea" name="description" rows="7" required></textarea></div><button class="button">Save task</button></form></section><section class="card"><h2>Accounts</h2>${state.accounts.map((account) => `<div class="item"><div><strong>${escapeHtml(account.name)}</strong><div class="muted">${escapeHtml(account.email)}</div></div><span class="badge">${account.accountType === 'administrator' ? 'Administrator' : 'Developer'}</span></div>`).join('')}</section><section class="card"><h2>Tasks</h2>${state.tasks.length ? state.tasks.map((task) => `<div class="item"><strong>${escapeHtml(task.name)}</strong><button class="button danger" data-delete-task="${task.id}">Delete</button></div>`).join('') : '<div class="empty">No tasks yet.</div>'}</section></div>`; }
function inboxView() { return `<div class="card"><h2>Review requests for me</h2><p class="muted">${state.inbox.length} pending request(s)</p><div class="inbox">${state.inbox.length ? state.inbox.map((request) => `<article class="review"><h3>${escapeHtml(request.temporary_name)}</h3><p>Task: ${escapeHtml(request.task_name)}</p><p><strong>Element to review:</strong> ${escapeHtml(request.element_to_review)}</p><p><strong>Deadline:</strong> ${escapeHtml(request.deadline)}</p><div class="actions"><button class="button" data-decision="done" data-request="${request.id}">✓ Review done by me</button><button class="button secondary" data-decision="not-concerned" data-request="${request.id}">I'm not concerned</button></div></article>`).join('') : '<div class="empty">No review requests yet.</div>'}</div></div>`; }
function startView() { return `<div class="card"><h2>Start a review</h2><form id="review-form" class="stack"><div class="field"><label>Company task</label><select class="select" name="taskId" required><option value="">Select a task</option>${state.tasks.map((task) => `<option value="${task.id}">${escapeHtml(task.name)}</option>`).join('')}</select></div><div class="field"><label>Temporary task name</label><input class="input" name="temporaryName" required></div><div class="field"><label>Element to be reviewed</label><textarea class="textarea" name="elementToReview" rows="3" placeholder="Link, document, repository path, or code location" required></textarea></div><div class="field"><label>Review deadline</label><input class="input" name="deadline" type="date" required></div><div class="field"><label>Reviewers</label><select class="select" name="reviewerIds" multiple required>${state.accounts.filter((account) => account.accountType === 'developer').map((account) => `<option value="${account.id}">${escapeHtml(account.name)} · ${escapeHtml(account.email)}</option>`).join('')}</select></div><button class="button">Send review request</button></form></div>`; }
function bindView() { document.querySelector('#account-form')?.addEventListener('submit', async (event) => { event.preventDefault(); try { await api('/accounts', { method:'POST', body:JSON.stringify(formData(event.target)) }); await renderDashboard(); } catch(error) { setError(error.message); } }); document.querySelector('#task-form')?.addEventListener('submit', async (event) => { event.preventDefault(); try { await api('/tasks', { method:'POST', body:JSON.stringify(formData(event.target)) }); await renderDashboard(); } catch(error) { setError(error.message); } }); document.querySelectorAll('[data-delete-task]').forEach((button) => button.onclick = async () => { await api(`/tasks/${button.dataset.deleteTask}`, { method:'DELETE' }); await renderDashboard(); }); document.querySelector('#review-form')?.addEventListener('submit', async (event) => { event.preventDefault(); const data=formData(event.target); data.reviewerIds=Array.from(event.target.elements.reviewerIds.selectedOptions).map((option)=>option.value); try { await api('/review-requests', { method:'POST', body:JSON.stringify(data) }); await renderView('home'); } catch(error) { setError(error.message); } }); document.querySelectorAll('[data-decision]').forEach((button) => button.onclick = async () => { await api(`/review-requests/${button.dataset.request}/decision`, { method:'POST', body:JSON.stringify({ decision:button.dataset.decision }) }); state.inbox=await api('/review-requests/inbox'); await renderView('home'); }); }
boot();
